#include <random>
#include <iostream>

int main(){
    // static std::random_device rd;
    // static std::mt19937 gen(rd());
    // std::uniform_int_distribution<> x_dist(0, 100);
    // std::uniform_int_distribution<> y_dist(0, 100);

    // int x = x_dist(gen);
    // int y = y_dist(gen);

    // std::cout << "x: " << x << " y: " << y << std::endl;

}


